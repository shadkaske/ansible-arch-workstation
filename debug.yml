---
- name: Arch Workstation Install
  hosts: arch
  remote_user: shadkaske
  become: true

  tasks:
    - name: Spit out user name
      ansible.builtin.debug:
        msg: "{{ dotfiles_path }}"

# vim: ft=yaml.ansible
